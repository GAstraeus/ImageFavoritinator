pillow>=10.0

# Optional but strongly recommended for fast CR3 browsing.
# Without it, the viewer falls back to macOS's built-in `sips` (slower).
rawpy>=0.19

# Optional: enables --native, the pixel-exact macOS backend (Apple's ImageIO
# decoder plus real pinch/scroll zoom). Sharper than tkinter on a Retina
# display, and decodes CR3 without rawpy.
pyobjc-framework-Quartz>=9.0
